__pycache__/
*.pyc
dist/
My existing website design files/
scripts/sync_cv.config.json
# LMS: keep built course packages, but never commit secrets
!lms/dist/
lms/docker/.env
# LMS generated media: the draft MP4s are committed so the GitHub Pages build can publish /learn/ with videos
lms/dist/*-with-media.imscc
lms/dist/preview/*/media/
